# Geek-Crawler v2 operator UI

Localhost Next.js App Router UI. See root [README.md](../README.md) for Phase 1 vs Phase 2.

```bash
cp .env.example .env.local   # optional until Phase 2
npm run dev
```

Or from repo root: `npm run web:dev`.
